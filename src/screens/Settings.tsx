import styled from "styled-components";

const Container = styled.div``;
const TItle = styled.h1``;
const Desc = styled.p``;

const Settings = () => {
    return <Container>
        <TItle>Settings</TItle>
        <Desc>This is Settings Page</Desc>
    </Container>
};

export default Settings;